namespace = HGTS_COL

planet_event = {
    id = HGTS_COL.1
    hide_window = yes
    is_triggered_only = yes

    immediate = {
        if = {
            limit = {
                any_pop = {
                    has_trait = hgts_better_nascent
                }
            }


            add_modifier = {
                modifier = hgts_childguidance
                mult = value:hgts_child_ratio
                days = 31 #31, month is 30 days, but I dont trust that stellaris and it might month tick for free :/
            }
        }
        ## nothing happens in else
    }
}
